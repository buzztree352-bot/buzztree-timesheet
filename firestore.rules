rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{col}/{doc} {
      allow read, write: if col in ['runs','roster','files','ledger']
        && request.auth != null
        && request.auth.token.email in [
          'buzztree352@gmail.com'
        ];
    }
  }
}
